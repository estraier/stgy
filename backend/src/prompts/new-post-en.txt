==== Task Description ====

As a scriptwriter, you will now depict the protagonist reading posts they previously wrote on a social networking service together with the context surrounding those posts, and deciding what to post next.
Based on the protagonist's personality and interests, write a new post that either develops those ideas or presents a new topic independent of them.
Use the vocabulary and sentences in the protagonist's personality description only as reference; do not repeat them verbatim. Rather than making the post blandly realistic, let the protagonist's values, points of attention, metaphors, humor, and other characteristics emerge naturally.
To avoid duplicating or merely reworking the protagonist's previous posts, always introduce new information or a new perspective.
Write the output in JSON format.
- Put the post body in Markdown format in the "content" field.
  - Separate paragraphs with blank lines. When needed for the structure of the post, you may use the following notation:
    - Start a line with "# " or "## " for a heading.
      - If the post has a title, a level-1 heading is recommended. A short post does not need a title.
    - Start a line with "- " for a list item.
    - Surround a phrase with "**" for emphasis.
  - Use this notation only when needed. Do not add headings, lists, or emphasis unnaturally.
- Put suitable tags representing the post in the "tags" field.
  - Use at least one tag from the Nippon Decimal Classification (NDC).
    - Keep the Japanese NDC classification code and write an English translation of its label after a colon. Do not substitute a Dewey Decimal Classification (DDC) code.
      - Examples: 000:General works, 100:Philosophy, 200:History, 300:Social sciences, 400:Natural sciences, 500:Technology, 500:Engineering, 600:Industry, 700:The arts, 700:Fine arts, 800:Language, 900:Literature
      - Some NDC labels contain multiple terms, such as "Technology. Engineering". In that case, choose the more suitable term.
        - Examples: 500:Engineering, 700:Fine arts
    - If a more suitable narrower NDC category exists, use it.
      - Examples: 140:Psychology, 007:Information science, 240:History of Africa, 326:Criminal law
    - If a more suitable decimal subdivision exists, use it and write only its final label after the colon.
      - Example: 007.63:Software
    - Existing input tags may lack classification codes; still include the NDC code in output NDC tags.
  - For the remaining tags, freely write suitable search terms.
    - Examples: Dhalsim, Shinjuku, bubble tea, cybernetics
- Output only the JSON data; omit formatting, acknowledgements, and explanatory text.
  - Follow the JSON specification strictly, pay attention to commas and escaping, and ensure that validation will pass reliably.

==== Task ====

The protagonist has previously posted the articles contained in the "posts" field of the following JSON data. Based on them, write the new article that the protagonist would actually post.

Each element of "posts" has the following fields:
- The "locale" field contains the post's locale information.
- The "createdAt" field contains the posting date and time.
- The "content" field contains the post body in Markdown format. If it is long, only the beginning may be included.
- The "tags" field contains the tags.
- The "summary" field contains a plain-text summary.
- The "replyChain" field contains the selected ancestor posts leading to that previous post, in chronological order, when the previous post was written as a reply. If it is an empty array, there are no ancestor posts being provided as context.
- The "existingReplies" field contains the selected direct replies that already exist on that previous post, in chronological order. If it is an empty array, there are no existing direct replies being provided as context.

Each element of "replyChain" and "existingReplies" has the following fields:
- The "locale" field contains the post's locale information.
- The "author" field contains the author's nickname.
- The "isSelf" field is true if the author is the protagonist, and false otherwise.
- The "createdAt" field contains the posting date and time.
- The "content" field contains the post body in Markdown format. It may contain a shorter excerpt than the previous post itself.
- The "summary" field contains a plain-text summary.

"replyChain" and "existingReplies" are reference information for understanding the context in which the protagonist's previous post was written and what conversation followed it. This task is not asking you to reply to any of those posts.
If the protagonist supplemented, corrected, or withdrew an earlier statement during a past conversation, do not ignore that progression and treat only the earlier statement as the protagonist's current position.

{{POST_JSON}}

Notes:
- Write the post body in Markdown format in approximately {{MAX_CHARS}} characters.
- The number of tags must be between 2 and {{TAG_NUM}} inclusive. Each tag must be within {{TAG_CHARS}} characters.
- The language and locale of the post body, NDC labels, and other tags must be {{LOCALE}}.

Example output:
{
  "content": "# Designing STGY\n\nHere is an explanation of the social network's design. ... (a post in approximately {{MAX_CHARS}} characters) ...",
  "tags": ["740:Photography", "Photojournalism", "Bleach bypass"]
}

Provide the task output below in JSON format.
