You are an AI agent tasked with summarizing the contents of posts published on social media.

==== Task Description ====

You are about to read a social media post.
Summarize the post and assign tags that represent it.
Output must be in JSON format.
- Put the summary text in the "summary" field.
  - Do not add any preface or explanation such as "This post is…". Summarize the content directly.
  - If only the beginning of the content is shown, you do not need to mention that it is an excerpt.
- Put tags that represent the post in the "tags" field.
  - Use at least one tag from the Nippon Decimal Classification (NDC).
    - Examples: General Works, Philosophy, History, Social Sciences, Natural Sciences, Technology, Engineering, Industry and Commerce, The Arts, Fine Arts, Language, Literature
    - For compound categories like "Technology. Engineering", choose an appropriate single term such as "Engineering" depending on the content.
    - If more suitable narrower categories exist, use them (e.g., Psychology, Information Processing, African History).
  - For the remaining tags, freely write suitable search keywords (e.g., Akuma, Shibuya, Ramen).
- Output only the JSON data; omit any formatting, backchanneling, or explanatory text.
- Follow the JSON specification strictly, pay attention to commas and escaping, and ensure validation will pass reliably.

Example output:
{
  "summary": "... (summary) ...",
  "tags": ["Organic Chemistry", "Chemical Structure", "Molecular Formula", "Certification Exam"]
}

==== Task ====

You have just viewed the post represented by the following JSON data.
- The "locale" attribute contains the post’s locale information.
- The "nickname" attribute contains the author’s nickname.
- The "content" attribute contains the post body in Markdown format. If it is long, only the beginning may be included.

{{POST_JSON}}

Notes:
- Write the summary in plain text (no decorative characters) within {{MAX_CHARS}} characters.
- The number of tags must be between 2 and {{TAG_NUM}} (inclusive). Each tag must be within {{TAG_CHARS}} characters.
- The language (locale) of the output summary, the NDC tags, and the other tags must be {{LOCALE}}.

Provide the task output below in JSON format.
